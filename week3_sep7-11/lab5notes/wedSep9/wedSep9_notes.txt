Exam 1 is Wednesday, September 16


Project 3 due Friday. Check file/folder names - 
    will be a deduction for naming.

    proj3
    Proj3.java


**Descriptive error messages
**Packages


///

Prime factorization of 12?

12 = 2*2*3


Prime factorization of 7?

7 = 7


Finding prime factorization:

num: user input
factor: start at 2
leftover: num

while leftover > 1
    if leftover % factor == 0
        print factor
        divide leftover by factor 
    else:
        factor++



How to tell if prime? 
    could introduce a count of how many factors you found
    or, the last factor you checked would be the number itself


How to find most frequent factor?

recommend having a current factor count that you update
in the if and the else


    keep a most frequent factor count before loop (start at 0)
    keep a current factor count (start at 0)
    in if (factor appeared again): increase current factor count
    in else (factor done):
        if current factor count is bigger than most frequent:
            update most frequent
        reset current factor count to 0 (to count the next factor)


/////////////////////////

for loops - redo factorial example

//////////////////////////

Do-while loops.

do {
    statements
} while (condition);


How are they different from while loops?
    does the statements BEFORE the condition 

Best situations to use a do-while?
    menu loops, etc. 


////////////////////////////

break - exits the loop

//What does this do?

int x = 0;
int y = 0;
do {
    x++;
    if (x == 1) {
        break;
    }
    y+=x;
} while (x < 5);
System.out.println(y);

y would be 0

///////////////////////////

continue - moves to the next iteration as long
    as condition is still true


//What does this do?

int x = 0;
int y = 0;
do {
    x++;
    if (x == 1) {
        continue;
    }
    y+=x;
} while (x < 5);
System.out.println(y);

would skip adding 1, but would add the others 

////////////////////////////

Random numbers.

import?

import java.util.*;

create random number generator?

Random r = new Random();



generate random number 0-4?

int val = r.nextInt(5);



///////////////////

Look at lab. 
- rolling a dice?

int dice = r.nextInt(6)+1;

- making it pause before dice rolls?

s.nextLine();