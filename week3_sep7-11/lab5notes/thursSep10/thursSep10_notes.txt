Exam 1 is Thursday, September 17


Project 3 due Friday. Check file/folder names - 
    will be a deduction for naming.

    proj3
    Proj3.java


**Descriptive error messages
**Packages


///

Prime factorization of 12?

12 = 2*2*3


Prime factorization of 7?

7 = 7


Finding prime factorization:

num: user input
factor: start at 2
leftover: num

while we're not done factoring (while leftover > 1)
    //can factor be pulled out of leftover?
    if leftover % factor == 0:
        print factor
        update leftover to divide out factor
    else:
        factor++


How to tell if prime? last factor you checked is the number itself


How to find most frequent factor?

keep track of most frequent factor
keep track of count for the current factor
while we're not done factoring (while leftover > 1)
    //can factor be pulled out of leftover?
    if leftover % factor == 0:
        print factor
        update leftover to divide out factor
        increase current factor count
    else:
        if my current factor count > my most frequent factor count:
            update most frequent to be the current count
        factor++
        reset current factor count to 0


/////////////////////////

int count = 0;
while (count < 5) {
    System.out.println("hello");
    count++;
}

--> how to write as a for loop?

for (int count = 0; count < 5; count++) {
    System.out.println("hello");
}




--> redo factorial example

//////////////////////////

Do-while loops.

do {
    statements
} while (condition);


How are they different from while loops?
    always executes statements at least once


Best situations to use a do-while?
    menus, etc.


////////////////////////////

What is this code doing?


int x = 0;
int y = 0;
do {
    x++;
    y+=x;
} while (x < 5);
System.out.println(y);

////////////////////////////

How does this behave differently?


int x = 0;
int y = 0;
do {
    x++;

    //added this if-statement
    if (x == 1) {
        break;
    }

    y+=x;
} while (x < 5);
System.out.println(y);

break leaves the loop

///////////////////////////

How does this behave differently?


int x = 0;
int y = 0;
do {
    x++;

    //added this if-statement
    if (x == 1) {
        continue;
    }

    y+=x;
} while (x < 5);
System.out.println(y);

continue skips the rest of the iteration
this will add numbers 2-5, skipping 1


////////////////////////////

Random numbers.

import?

import java.util.*;


create random number generator?

Random r = new Random();



generate random number 0-4?

int val = r.nextInt(5);



///////////////////

Look at lab. 
- rolling a dice?

int dice = r.nextInt(6)+1;

- making it pause before dice rolls?


