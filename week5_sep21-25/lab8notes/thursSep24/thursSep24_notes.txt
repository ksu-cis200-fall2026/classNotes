Project 4 due Friday


How to store temp values?

array for low temps
array for high temps



Calculating dates

    //assume "start" is: "12/28/25";
    String[] pieces = start.split("/");
    int[] startDate = new int[pieces.length];
    for(int i = 0; i < pieces.length; i++) {
        startDate[i] = Integer.parseInt(pieces[i]);
    }

    What is in startDate now? 


///////////////

int[] days = {31,28,31,30,31,30,31,31,30,31,30,31};


Store dates in array (recommend string array)
    how big?


What is in startDate now? 

month = startDate[0]
day = startDate[1]
year = startDate[2]


Now create dates array:

create String array to hold the dates (same size as the timespan)
loop through each date to generate
    create date month/day/year, put in array
    now generate tomorrow's date
        are we at the end of the year?
        (is month 12 and day 31?)
            month = 1
            day = 1
            year++
        are we at the end of a month?
            (is days[month-1] == day)
            day = 1
            month++
        otherwise:
            day++




/////////////////////////////

Two-dimensional arrays (Grid.java).




///////////////////////////

Lab. 

1) Create a 2D array with values 1, 2, ... that can be 
"read" like a book

2) left-shift each row until user wants to stop


How to left-shift?
    {1,2,3,4} becomes?
    (in general) update each position to be...?

What kind of loops are needed?

outer loop: menu loop while they want to keep going
    want to do a left shift
    loop through each row (i)
        oldFront = grid[i][0]
        loop through each column (j) (while < cols-1)
            replace grid[i][j] with one next to it (grid[i][j+1])
        update last element in row:
            grid[i][cols-1] = oldFront


Reminder: switching between types of input



