Lab 2: today

//////////////////////////

Can get help setting up your computer in office hours

///////////////////////////

Project 1 due Friday, August 28
    --> what to do?

    --> how to check submission?




Documentation template:

/**
 * (description of project)
 *
 * @author (your name)
 * @version (which project number this is)
 */

//////////////////////////

What to do for next week?

//////////////////////////

System.out.println("text");
System.out.println();
System.out.print("\ttext\n");
System.out.println("another");

What prints?

text
    text
another



//////////////////////////

Variables.

Data types in Java.

    int - integer (whole number)
    double - number with a decimal (can be a whole)
    String - text 
    char - single character
    boolean - true/false

Declaring variables.

    type name;


    int age;




Initializing/assign variables.

    name = literal/expression/other variable;

    age = 17;

    age *= 2;  //age is now 34
    age = age * 2;

    age++;  //adds 1, age is now 35
    age = age + 1;
    age += 1; 


    int x = 7;
    int y = 4;
    int num = x - y*2; //num is -1

()
multiplication/division
addition/subtraction

//////////////////////////

User input.

1) At the very top of the file:

import java.util.*;


2) At the beginning of the main method (before 
any user input):

Scanner s = new Scanner(System.in);



3) For each piece of user input:

//print a prompt
System.out.print("Enter something: ");

//read in what they type
//for an int
int num = s.nextInt();

//for a double
double val = s.nextDouble();


//for a String (text)
String text = s.nextLine();


//////////////////////////

Formatted printing.

Prints:
I am 17 years old

int age = 17;
System.out.printf("I am %d years old%n", age);

%d - int
%f - double
%s - String
%c - char





int num = 3;
int denom = 4;

//Want to print:
//Fraction: 3/4
System.out.printf("Fraction: %d/%d%n", num, denom);


double pi = 3.14159;

//Want to print:
//Pi rounded is 3.14
System.out.printf("Pi rounded is %.2f%n", pi);

//////////////////////////

Integer division.

    int result = 17/4; //result is 4

    double result = 17.0/4; //4.25



Modulo.

    int leftover = 17 % 4; //leftover is 1


//////////////////////////

Example: pizzas have 8 slices. 
Get #pizzas, #people. 

Print how many slices everyone can have, 
how many are leftover.

Suppose pizzas are $12.99 each, print total.


//////////////////////////

how to find class notes

/////////////////////////

Lab 2.

Donuts sold in boxes of 12.
Get #donuts needed from user (assume positive whole number).
Calculate minimum boxes needed, how many will be leftover.

donutsNeeded = (user input);
boxes = (donutsNeeded-1)/12 + 1;

